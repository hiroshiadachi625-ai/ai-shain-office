# AI社員オフィス 設計書(実装前たたき台)

前提: 要件定義書の決定事項(A/A/A)
- APIキー: 画面入力→ブラウザ`localStorage`保存
- 成果物保存: File System Access API(フォルダ許可を最初に1回)。非対応ブラウザは個別ダウンロードにフォールバック
- 初期スコープ: モックデータでUI/演出を先行実装し、API呼び出し部分はあとから差し込む

## 1. ファイル構成

```
AI社員オフィス/
  index.html          … 単体ファイル(HTML+CSS+JS)。これが成果物本体
  設計書.md            … 本ファイル
```

出力(成果物ファイル)は実行時にユーザーが許可したフォルダの下に、
`<プロジェクト名>/<部署名>/<連番>_<ファイル名>` という構造で保存する。
(フォルダ選択前は「画面内表示のみ」の状態で動作する)

## 2. データモデル(JSでオブジェクト定義、外部ファイル分離なし)

```js
// 部署マスタ(初期テンプレート、設定画面で編集可能)
const departments = [
  { id: "dev",   name: "開発部",     icon: "🖥", description: "システム実装・技術検証" },
  { id: "sales", name: "営業部",     icon: "🤝", description: "現場の使い勝手・顧客反応" },
  { id: "plan",  name: "業務企画部", icon: "📋", description: "業務フロー設計・要件整理" },
  { id: "qa",    name: "品質ゲートウェイ班", icon: "✅", description: "仕様確定・品質基準" },
  // ...
];

// キャラクター(担当者)。部署に対して1人以上ひもづく
const characters = [
  {
    id: "tanaka",
    name: "田中",
    deptId: "dev",
    personality: "出世競争",
    constraints: ["工数は3人月まで", "過去の障害事例A"],
    earlyCompromiseBanned: true,
  },
  // ...
];

// プロジェクト実行状態(実行中に更新される)
const projectState = {
  title: "",
  overview: "",
  startedAt: null,
  stats: { activeCount: 0, exchangeCount: 0, artifactCount: 0, hirameki: 0 },
  deptStatus: {
    // deptId -> { status: "waiting"|"working"|"done", task: "現在の作業内容", progress: 0-100, collaboratingWith: [deptId,...] }
  },
  conflictCounters: {
    // "deptA|deptB" -> 往復回数。閾値超えで「ひらめき」トリガー
  },
};

// ログ(時系列、画面下に積み上げ)
// type: "message" | "thinking" | "hirameki" | "human-gate" | "artifact"
const logEntries = []; // { id, ts, type, from, to, content }

// 成果物
const artifacts = []; // { id, deptId, filename, content, createdAt }
```

## 3. オーケストレーション(部署間やり取りのロジック)

### 3.1 API呼び出しの分離(要件4.1準拠)
- 部署役ごとに独立したAPI呼び出しにする(全履歴を毎回渡さない)。
- 各呼び出しに渡す情報:
  - システムプロンプト: 自分の部署・性格・非公開の制約・早期妥協禁止ルール
  - ユーザープロンプト: 相手からの直近の発言/成果物のみ
- 全体ログは`logEntries`に裏側で集約し、ダッシュボードと社長役だけが俯瞰できる。部署役のプロンプトには含めない。

### 3.2 スタブ関数として先に定義(後でAPI実装に差し替え)
```js
// 段階1(モック)ではsetTimeout+ダミー文言、段階2でAnthropic API呼び出しに置き換える
async function callDepartmentAI(character, incomingMessage, context) {
  // 戻り値: { text, proposedArtifact?: {filename, content} }
}
```

### 3.3 往復・対立カウントと「ひらめき」トリガー(要件3.4/4.3)
- 部署ペアごとに`conflictCounters["A|B"]`をインクリメント。
- 閾値(初期値: 3往復、設定画面で変更可)を超えたら、通常のやり取りを止めて統合プロンプトを1回呼び出す。
- 統合プロンプトには両者の直近の主張・制約(公開範囲のみ)を渡し、「ひらめき」カードとして専用スタイルで表示。
- ひらめき後はカウンターをリセットし、次のフェーズへ進む。

### 3.4 人間ゲート(要件4.4)
- ワークフロー定義上、特定のステップに`requiresHumanGate: true`を付与。
- そのステップに到達すると自動進行を止め、モーダルで「現場の反応」等のテキスト入力を求める。
- 入力が送信されるまで後続の部署呼び出しは発火しない(ボタン非活性)。

## 4. 画面構成とコンポーネント

1. **ヘッダー**: 小ラベル+大見出し(プロジェクトタイトル)
2. **統計カード(2列グリッド)**: 稼働人数/やり取り件数/成果物数/ひらめき件数
3. **部署一覧リスト**: アイコン+部署名+現在の作業(クリックで詳細パネル)。連携中は枠を太いアクセントカラーで強調。
4. **部署詳細パネル**: 名前・担当分野・進捗バー・状態別スタイル(完了=落ち着いた色、待機=薄い点線)
5. **やり取りログ(チャット風)**: 発言者名+吹き出し、思考中は「検討中…」表示、下に積み上がりスクロール可
6. **ひらめきカード**: 専用の強調色+電球アイコンで通常ログと視覚的に区別
7. **成果物ファイル一覧**: アイコン+ファイル名、クリックで内容プレビュー
8. **人間ゲートモーダル**: テキスト入力+送信ボタン、送信まで後続処理をブロック
9. **設定画面**: 部署/キャラクター編集(初期テンプレートあり)、APIキー入力欄、保存先フォルダ選択ボタン

## 5. 実装の段階分け

### 段階1(今回着手): モックデータでUI/演出を先行実装
- 上記データモデルをダミー値で用意し、`setInterval`等で自動的にログ・進捗・ひらめきが発生するデモを作る。
- `callDepartmentAI`はスタブ(ダミー文言をランダム返却)。
- File System Access APIの権限フローとフォールバック(ダウンロード)は実装するが、保存内容はダミーで確認。
- ゴール: 画面の見た目・演出・情報設計についてユーザーと合意を取る。

### 段階2(次回以降): 実API接続
- `callDepartmentAI`をAnthropic API呼び出しに置き換え(`anthropic-dangerous-direct-browser-access`ヘッダーでブラウザから直接呼び出し)。
- モデル: Claude Sonnet 5想定(設定で変更可能にする余地は残す)。
- エラーハンドリング(レート制限・タイムアウト)をログにカード表示。
- 部署2〜3×性格2〜3程度の最小構成でまず動作確認。

## 6. 未確定・要相談事項

- 部署間の呼び出し順序(ワークフロー)をどこまで固定シナリオにするか、汎用エンジンにするかは段階2で検討。
- Claude APIのレート制限・コストの上限をどう画面上に見せるか(例: 呼び出し回数カウンタ)は要検討。
- 成果物のファイル形式(テキスト/コード/簡易資料)ごとの拡張子・プレビュー方法は段階2で詳細化。
